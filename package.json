{
  "name": "cancha-libre",
  "version": "1.0.0",
  "private": true,
  "description": "Reservas de canchas de fútbol 5 — proyecto ejemplo del curso 'GS en la práctica' (PragmaWorks)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "gate": "sh .githooks/gate-componible.sh && sh .githooks/gate-ids.sh",
    "api": "node --experimental-transform-types src/api/servidor.ts",
    "probes": "node --experimental-transform-types probes/correr.ts",
    "integracion": "node --experimental-transform-types tests/integracion/tres-capas.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.2.7"
  }
}
